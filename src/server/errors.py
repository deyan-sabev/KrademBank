ERRORS = {
    # =========================
    # 600 – грешки на банката на наредителя
    # =========================
    601: "Недостатъчна наличност в сметката на наредителя.",
    602: "Банката наредител не може да се върже към банковия регистър.",
    603: "Не сте нито наредител, нито бенефициент. Може би има грешка.",
    604: "Надвишавате си лимита за еднократно плащане.",
    605: "Невалидна валута.",
    606: "Липсващи данни.",
    607: "Наредителят и получателят трябва да бъдат различни.",
    608: "Невалиден IBAN.",
    609: "Невалидна сметка на наредителя.",

    # =========================
    # 650 – грешки от банката бенефициент
    # =========================
    651: "Банката наредител не може да се свърже с банката бенефициент.",
    652: "Невалидна сметка на бенефициента.",

    # =========================
    # 700 – грешки от банковия регистър
    # =========================
    701: "Неразпозната банка на бенефициента."
}

HTTP_STATUS = {
    601: 400, 602: 502, 603: 403, 604: 400, 605: 400, 606: 400,
    607: 400, 608: 400, 609: 404, 651: 502, 652: 404, 701: 404
}

def is_valid_iban(iban: str) -> bool:
    return bool(iban) and len(iban.strip()) <= 22 and iban.isalnum() and iban[:3].isalpha()

class APIError(Exception):
    def __init__(self, code, message=None):
        self.code = code
        self.message = message or ERRORS.get(code, "Неразпознат код за грешка.")
        self.http_status = HTTP_STATUS.get(code, 400)
        super().__init__(self.message)
